project:
  module:
    documents:
      'qmldir': 'qmldir'
      'private/qmldir': 'private/qmldir'
      'private/{{module_name}}Module.js': 'private/module.js'
  interface:
    documents:
      'private/Abstract{{interface}}.qml': 'private/AbstractInterface.qml'
    preserve:
      '{{interface}}.qml': 'Interface.qml'
